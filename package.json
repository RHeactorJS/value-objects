{
  "name": "@rheactorjs/value-objects",
  "description": "A collection of value objects",
  "version": "0.0.0-development",
  "main": "dist/index.js",
  "scripts": {
    "test": "NODE_ENV=testing mocha --compilers js:babel-register --reporter=dot --timeout=1350 test/\\*\\*/\\*.js",
    "test:coverage": "NODE_ENV=nyc babel src --out-dir instrumented-src > /dev/null && nyc --reporter=text-summary --reporter=lcov npm run test",
    "test:coverage-travis": "npm run test:coverage && codeclimate-test-reporter < ./coverage/lcov.info && rm -rf ./coverage",
    "lint": "standard",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post",
    "precommit": "npm run lint && npm test",
    "commitmsg": "commitlint -e"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RHeactorJS/value-objects.git"
  },
  "author": "Resourceful Humans GmbH <info@resourceful-humans.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/RHeactorJS/value-objects/issues"
  },
  "homepage": "https://github.com/RHeactorJS/value-objects#readme",
  "dependencies": {
    "tcomb": "^3.2.13",
    "@rheactorjs/errors": "1.x"
  },
  "devDependencies": {
    "@commitlint/cli": "^4.2.0",
    "@commitlint/config-angular": "^4.2.0",
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.1",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "chai": "^4.1.1",
    "codeclimate-test-reporter": "^0.5.0",
    "husky": "^0.14.3",
    "istanbul": "^0.4.4",
    "mocha": "^4.0.0",
    "mocha-lcov-reporter": "^1.2.0",
    "nyc": "^11.1.0",
    "semantic-release": "^8.0.0",
    "standard": "^10.0.0"
  },
  "files": [
    "dist",
    "src",
    "test",
    "README.md"
  ],
  "engines": {
    "node": ">=6.10.2"
  }
}
